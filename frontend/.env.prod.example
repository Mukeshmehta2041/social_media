# Example production env for frontend
# Copy to `frontend/.env.prod` and fill values before building or running.

# URL where API is exposed (used at runtime/build-time by Vite)
VITE_API_BASE_URL=https://api.yourdomain.com/api
